# Image Cache Plugin Example

This Android example app currently provides the project foundation for the
`image_cache_plugin` package. Image loading and gallery behavior will be added
in later milestones.

Run it from this directory with:

```bash
flutter pub get
flutter run
```
