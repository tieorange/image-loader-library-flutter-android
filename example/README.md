# Image Cache Plugin Example

This Android gallery demonstrates the plugin, MVVM state management, clean
layer boundaries, item-level image failures, and native cache clearing. See the
[root README](../README.md) for architecture, API, cache, and verification
details.

Run from this directory on an Android device or emulator:

```bash
flutter pub get
flutter run
```
